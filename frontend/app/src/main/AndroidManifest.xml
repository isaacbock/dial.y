<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.twilliodemo">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.RECORD_AUDIO" />

    <application
        android:allowBackup="true"
        android:background="@color/background"
        android:icon="@drawable/dialing"
        android:label="@string/app_name"
        android:roundIcon="@drawable/dialing"
        android:supportsRtl="true"
        android:theme="@style/Theme.TwillioDemo">
        <activity
            android:name=".ChooseLanguage"
            android:exported="true" />
        <activity
            android:name=".ChooseAppLanguage"
            android:exported="true" />
        <activity
            android:name=".MainActivity"
            android:exported="false" />
        <activity android:name=".LoginScreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".CallPage" />
        <activity android:name=".StartCall">

            <!-- <intent-filter> -->
            <!-- <action android:name="android.intent.action.MAIN" /> -->
            <!-- <category android:name="android.intent.category.LAUNCHER" /> -->
            <!-- </intent-filter> -->
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>